{
  "name": "dyor-sh",
  "version": "0.1.0",
  "private": true,
  "description": "AI research shell for crypto token due diligence.",
  "scripts": {
    "dev": "wrangler pages dev .",
    "build": "node scripts/check-static.mjs",
    "check": "node scripts/check-static.mjs",
    "deploy": "wrangler pages deploy . --project-name dyor-sh --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
