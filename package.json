{
  "name": "dyor-sh",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Native research shell for crypto token due diligence.",
  "scripts": {
    "dev": "wrangler pages dev .",
    "build": "node scripts/check-static.mjs && node scripts/build-dist.mjs",
    "check": "node scripts/check-static.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name dyor-sh --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
